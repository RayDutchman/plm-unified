# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/

# Node / Frontend
node_modules/
frontend/dist/
frontend/.env.local

# 环境变量
.env
.env.*
!.env.example
# 前端 mock 开关（非密钥，需入库供团队默认启用前端独立运行）
!frontend/.env.development

# Docker
docker/data/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code / OpenCode 个人本地设置（含权限模式，不入库）
.claude/settings.local.json
# Playwright MCP 生成的截图、快照、日志（临时产物，不入库）
.playwright-mcp/

# OS
.DS_Store
Thumbs.db

# vault（CAD 文件和 GLB，不入库）
/vault/

# Alembic
alembic/versions/__pycache__/
# conversion/wheels/ 由 Git LFS 管理（已移除 ignore 规则）
# conversion-service/target/ 的 *.jar 由 Git LFS 管理（已移除 ignore 规则）
# conversion/conversion-service/target/ 中的非 jar 文件（classes/ 等编译中间产物）仍排除
conversion/conversion-service/target/classes/
conversion/conversion-service/target/generated-sources/
conversion/conversion-service/target/maven-archiver/
conversion/conversion-service/target/maven-status/
